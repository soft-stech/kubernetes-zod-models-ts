---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: flags.features.bootsman.tech
spec:
  group: features.bootsman.tech
  names:
    kind: Flag
    listKind: FlagList
    plural: flags
    singular: flag
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Flag is the Schema for the addons API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FlagSpec defines the desired state of Flag.
            properties:
              defaultValue:
                description: DefaultValue shows if flag is enabled by default.
                type: boolean
              value:
                description: Value defines customized flag state.
                type: boolean
            required:
            - defaultValue
            type: object
          status:
            description: FlagStatus defines the observed state of Flag.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
