---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
    crd.bootsman.tech/dependent-addons: "longhorn:All"
    crd.bootsman.tech/dependent-addons-config: "Longhorn:All"
    crd.bootsman.tech/required-in-clusters: ""
  labels:
    clusterctl.cluster.x-k8s.io/move: ""
    crd.bootsman.tech/addon-name: logging
    crd.bootsman.tech/target-cluster-type: All
    crd.bootsman.tech/type: Addon
  name: loggings.configuration.bootsman.tech
spec:
  group: configuration.bootsman.tech
  names:
    categories:
    - bootsman
    kind: Logging
    listKind: LoggingList
    plural: loggings
    singular: logging
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Logging is the Schema for the Logging API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoggingSpec defines the desired state of Logging
            properties:
              disabled:
                default: false
                type: boolean
              logConfig:
                properties:
                  compactor:
                    properties:
                      retentionDeleteDelay:
                        description: |-
                          Duration is a valid time duration.
                          Supported units: y, w, d, h, m, s, ms
                          Examples: `30s`, `1m`, `1h20m15s`, `15d`
                        pattern: ^(0|(([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?)$
                        type: string
                    required:
                    - retentionDeleteDelay
                    type: object
                  limitsConfig:
                    properties:
                      retentionPeriod:
                        description: |-
                          Duration is a valid time duration.
                          Supported units: y, w, d, h, m, s, ms
                          Examples: `30s`, `1m`, `1h20m15s`, `15d`
                        pattern: ^(0|(([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?)$
                        type: string
                    required:
                    - retentionPeriod
                    type: object
                type: object
              loki:
                properties:
                  persistence:
                    properties:
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                type: object
            type: object
          status:
            description: LoggingStatus defines the observed state of Logging
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
